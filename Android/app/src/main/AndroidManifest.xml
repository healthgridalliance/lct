<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.openar.healthgrid">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".HealthGridApplication"
        android:allowBackup="false"
        android:fitsSystemWindows="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.splash">

        <activity
            android:name=".ui.activity.welcome.WelcomeActivity"
            android:theme="@style/AppTheme.base"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".ui.activity.map.MapsMainActivity"
            android:theme="@style/AppTheme.base"
            android:configChanges="keyboardHidden"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".ui.activity.exposure.ExposureActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <provider
            android:name=".database.LocationInfoProvider"
            android:authorities="com.openar.healthgrid.database.LocationInfoProvider"
            android:exported="false" />

        <service android:name=".service.LocationTrackingService" />
    </application>

</manifest>